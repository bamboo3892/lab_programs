from sklearn.cross_decomposition import CCA


def CCA_between_thetas(model1, model2, model1_name, model2_name):
    pass


def extract_theta(model, name):
    if(name == "MCLDA"):


    elif(name == "LLDA"):
        pass
